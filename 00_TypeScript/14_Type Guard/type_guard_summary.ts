/*
🎯 **TypeScript Type Guards - Summary**
--------------------------------------------
✅ **What are Type Guards?**
   - A way to determine the specific type at runtime.
   - Used to **avoid TypeScript errors** when handling multiple types.

✅ **Types of Type Guards:**
   1️⃣ `typeof` - Checks primitive types.
   2️⃣ `instanceof` - Checks class instances.
   3️⃣ `in` operator - Checks if a property exists in an object.
   4️⃣ Custom type predicates - Functions returning `animal is Type`.
   5️⃣ Discriminated unions - Uses a common property (`kind`) to differentiate types.

🚀 **Why Use Type Guards?**
✔️ Ensures type safety in complex structures.
✔️ Reduces runtime errors.
✔️ Helps TypeScript infer correct types dynamically.
*/
