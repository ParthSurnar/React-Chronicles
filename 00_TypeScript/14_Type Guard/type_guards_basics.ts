/*
📌 **TypeScript Type Guards - Overview**
--------------------------------------------
✅ Type Guards are used to **narrow down types** at runtime.
✅ They help TypeScript determine the **specific type** of a variable.
✅ Essential when working with `union` or `unknown` types.
✅ Implemented using:
   1️⃣ `typeof` check
   2️⃣ `instanceof` check
   3️⃣ Custom type predicates
   4️⃣ `in` operator
*/
