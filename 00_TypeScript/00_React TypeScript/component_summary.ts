/*
🎯 **React + TypeScript - Summary**
---------------------------------------
✅ **Types of Components in TypeScript React:**
   - Functional Components (`React.FC`)
   - Class Components (Less common in modern React)

✅ **Handling Props & State**
   - Use `interface` for props definition
   - Use `useState<Type>()` for type-safe state

✅ **Handling Events & DOM Elements**
   - Use `React.MouseEvent<HTMLButtonElement>` for events
   - Use `useRef<HTMLInputElement>()` for DOM refs

✅ **Advanced Features**
   - Context API (`createContext`, `useContext`)
   - Custom Hooks (`useFetch<T>()`)
   - Higher-Order Components (HOC)

🚀 **Why TypeScript in React?**
✔️ Type Safety  
✔️ Better Code Readability  
✔️ Fewer Runtime Errors  
✔️ Improved Developer Experience  
*/
