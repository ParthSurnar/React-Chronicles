/*
📌 **TypeScript Modules - Overview**
--------------------------------------
✅ Modules help in organizing code by splitting it into different files.
✅ Each file in TypeScript is **automatically a module** when it has an `export` or `import`.
✅ `export` makes a variable, function, or class available for other files.
✅ `import` brings the exported module into another file.
*/
